version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: club_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: clubs_sportifs
     
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - club_network

  tomcat:
    image: tomcat:10.1
    container_name: club_tomcat
    restart: always
    depends_on:
      - mysql
    ports:
      - "8080:8080"
    volumes:
      - ./target/club-sport.war:/usr/local/tomcat/webapps/club-sport.war
    networks:
      - club_network

volumes:
  mysql_data:

networks:
  club_network: